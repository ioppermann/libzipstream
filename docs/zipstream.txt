#include "zipstream.h"

ZS *zs;

zs = zs_init();

zs_add_file(zs, "foo.txt");
zs_add_file(zs, "bar.txt");
zs_add_file(zs, "baz.doc");

/* later
zs_add_directory(zs, "foobar");
zs_add_file(zs, "foobar/biz.txt");
*/

zs_finalize();

while((bytes = zs_write(zs, buf, sizeof(buf)) > 0)
	fwrite(buf, 1, bytes, stdout);

zs_free(zs);

